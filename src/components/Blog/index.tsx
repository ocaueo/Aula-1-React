
type BlogProps = {
    titulo: string,
    conteudo: string,
    imagem?: string,
    fechar?: boolean,
}

const Blog = (titulo, conteudo, imagem, fechar} : BlogProps) => {
    return(
        <>
        <BlogTitulo
        </>
    );
}